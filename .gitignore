# Root ignores
# See: SHAREABLE_VS_INTERNAL_CLASSIFICATION.md for full context
# This .gitignore prevents INTERNAL files from being accidentally committed

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.build/
.venv/
venv/
.env
.env.*
!.env.example

# Node/JS
node_modules/
**/node_modules/
**/build/
**/.cache/

# OS
.DS_Store
Thumbs.db

# Secrets & Credentials (INTERNAL — NEVER commit these)
secrets/
secrets-local/
.secrets/
.keys/
local.settings.json
*.secret
*.secrets
*.key
*.pem
*.p12
*.cer
*.ppk
wp-config.php
wp-config-*.php
# Environment files with sensitive data
.env
.env.*
!.env.example
# GitHub/external credentials
*.bitwarden.json
mapping.json

# Local Development (INTERNAL — temporary build artifacts)
# Rebuild these locally after clone; don't commit
build/
dist/
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
.mypy_cache/
.ruff_cache/

# AI workflow artifacts (CONDITIONAL — sanitize before sharing)
ai-workflow/*.json
ai-workflow/output/
ai-workflow/generated/
ai-workflow/*.ipynb  # Notebooks may contain embedded credentials

# Archived/Deprecated (INTERNAL — keep local, don't share)
artifacts/luminai-archive/
scripts/archive/
*_ARCHIVE/
*_OLD/
*_DEPRECATED/
*_BACKUP/

# VS Code (track selected configuration files for cross-device unity)
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Data dumps and evidence (keep small samples under data/samples/)
data/evidence/
data/financial/
data/**/*.csv
!data/samples/**

# Docs exports and archives
docs/exports/
docs/archive/

# Build/distribution artifacts (root-level)
dist/
exports/
.wpcom-dist/

# Generated plugin archives
exports/wp-plugin/**/*.zip
*.wpcom.zip

# App build outputs
apps/**/dist/
apps/**/build/

# Logs and reports
*.log
logs/
reports/

# Python coverage and test artifacts
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Images/renders generated by tools
**/renders/**
**/*.png
**/*.jpg
**/*.jpeg
**/*.webp
!docs/**

# Audio/video large assets
**/*.wav
**/*.mp3
**/*.amr
**/*.mp4
**/*.mov

# 3D model binaries (keep out of repo; upload to WP Media Library or CDN)
**/*.glb
**/*.gltf

# Misc caches
.cache/
.parcel-cache/

# Todo scaffolds and temporary task files
todo/
.todo/
.git/todo/
tasks.json

# Local environment files (safe to ignore - do NOT commit secrets)
.env.local
.env.local.bak

# Ignore raw brain dump transcripts (sensitive / large)
brain_dump/
/secrets-local/

# Ignore generated ingest JSONs for brand personal notes
data/digital_assets/brand/ingests/
